export type NavItem = {
  id: number;
  title: string;
  icon: string;
  href: string;
  active: boolean;
};
export type RadioItem = {
  id: string;
  active: boolean;
  label: string;
  value: string;
};
